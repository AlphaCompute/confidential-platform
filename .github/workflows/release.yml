name: release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  image:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: alpha-runtime
            dockerfile: images/manager/Dockerfile
          # The KMS App: two CVMs, one Revision; the compose rendered below is that Revision.
          - name: alpha-kms
            dockerfile: images/kms/Dockerfile
            app_id: 01a09f07-8d09-7640-94c1-bbdb74200a1e
          # The dev KMS App with the root KEK from env; a distinct image, never a switch.
          - name: alpha-kms-dev
            dockerfile: images/kms/Dockerfile
            features: dev-root
            app_id: 01a09f07-8d09-7aff-b257-83dbd9e6e641
          # No app_id: the release workflow renders a compose only for the KMS.
          - name: alpha-inference
            dockerfile: images/inference/Dockerfile
          - name: alpha-broker
            dockerfile: images/broker/Dockerfile
          - name: alpha-guard
            dockerfile: images/guard/Dockerfile
    env:
      FEATURES: ${{ matrix.features }}
    steps:
      - uses: actions/checkout@v4
      - name: Build ${{ matrix.name }} twice, the second time from nothing
        run: |
          export SOURCE_DATE_EPOCH=$(git log -1 --format=%ct)
          build() {
            docker build --platform linux/amd64 --provenance=false \
              --build-arg SOURCE_DATE_EPOCH="$SOURCE_DATE_EPOCH" \
              ${FEATURES:+--build-arg FEATURES=$FEATURES} \
              -f "${{ matrix.dockerfile }}" -t "${{ matrix.name }}:$1" "${@:2}" .
          }
          build first
          build second --no-cache
          first=$(docker image inspect --format '{{.Id}} {{.RootFS.Layers}}' "${{ matrix.name }}:first")
          second=$(docker image inspect --format '{{.Id}} {{.RootFS.Layers}}' "${{ matrix.name }}:second")
          echo "first:  $first"
          echo "second: $second"
          if [ "$first" != "$second" ]; then
            echo "the two builds of ${{ matrix.dockerfile }} differ; the image is not reproducible" >&2
            exit 1
          fi
          echo "${{ matrix.name }} image (SOURCE_DATE_EPOCH=$SOURCE_DATE_EPOCH): $first" >> "$GITHUB_STEP_SUMMARY"
      - name: Push
        id: push
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
          image="ghcr.io/$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')/${{ matrix.name }}:${GITHUB_REF_NAME}"
          docker tag "${{ matrix.name }}:first" "$image"
          docker push "$image"
          digest=$(docker image inspect --format '{{index .RepoDigests 0}}' "$image")
          echo "pushed $digest" >> "$GITHUB_STEP_SUMMARY"
          echo "digest=$digest" >> "$GITHUB_OUTPUT"
      # The unsigned release manifest: the compose of the KMS node with this image, in the bytes
      # Phala measures, and its compose_hash for the release-key holder to sign into kms_revisions.
      - name: Render the KMS compose from the pushed digest
        if: matrix.app_id && startsWith(github.ref, 'refs/tags/')
        run: |
          {
            echo '```json'
            cargo run -q -p alpha-cli --example kms_compose -- ${FEATURES:+--dev-root} \
              "${{ matrix.app_id }}" "${{ steps.push.outputs.digest }}" "release/${{ matrix.name }}"
            echo '```'
            echo '```json'
            cat "release/${{ matrix.name }}/app-compose.json"
            echo
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@v4
        if: matrix.app_id && startsWith(github.ref, 'refs/tags/')
        with:
          name: ${{ matrix.name }}-release-manifest
          path: release/${{ matrix.name }}
          if-no-files-found: error
